# FantasyX

FantasyX is an unofficial fantasy football web app using virtual money only.

This repository is a source backup/migration of the Hatchable V44 project. The `public/` frontend can be hosted statically, while the current `api/`, `migrations/`, and `hatchable.toml` files depend on Hatchable and must be adapted before deploying to another backend host.
